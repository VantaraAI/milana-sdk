# Require packages to be published for at least 7 days before installation.
# Mitigates supply chain attacks — malicious releases are typically discovered
# and removed within hours/days of publication.
minimumReleaseAge: 10080
# Escape hatch for security patches younger than the cooldown. Scope each entry
# to the exact patched version so future releases stay quarantined, and remove
# the entry once that version has aged past the cooldown (the override/lockfile
# pin keeps it installed without the exclude).
minimumReleaseAgeExclude:
  # undici 7.28.0 fixes CVE-2026-9697 (high, TLS cert validation bypass via
  # SOCKS5 ProxyAgent) and CVE-2026-9678 (medium, shared-cache info disclosure).
  # Transitive via jsdom (^7.25.0), dev-only. Scoped to the exact patched
  # version; released 2026-06-15, clears the 7-day cooldown 2026-06-22.
  - undici@7.28.0
